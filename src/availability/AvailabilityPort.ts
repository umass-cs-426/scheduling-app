// This is the public interface for the Availability module. It defines the
// types and functions that are exposed to other modules in the application.
// The actual implementation of these functions will be defined in the
// AvailabilityRepository module.

// The AvailabilityPort interface defines the functions that are exposed by the
// Availability module to other modules in the system. Currently, there are no
// functions defined in this interface, but it can be extended in the future to
// include any functions that need to be exposed to other modules.
interface AvailabilityPort {
  // Currently, we do not expose any functions.
}
